import 'package:flutter_test/flutter_test.dart';

void main(){
  testWidgets('''
    Error muncul di login screen ketika field tidak valid,
    dan error hilang ketika field sudah valid,
  ''', (widgetTester) async {

  });
}