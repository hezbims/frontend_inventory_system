import 'package:flutter/material.dart';

class TransaksiDrawer extends StatelessWidget {
  const TransaksiDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return Drawer();
  }
}
